pipeline {
    agent any
        
    tools {
    maven 'maven' 
  }

    stages {
       stage("Git Checkout"){
          steps{
           git 'https://github.com/chenna6/amazon.git'
           }
          }
          
           stage("build"){
          steps{
           sh "mvn -f amazonweb/om.xml clean package"
           }
          }
        
        stage ('Deploy') {
      steps {
        script {
          deploy adapters: [tomcat9(credentialsId: 'tomcat', path: '', url: 'http://http://65.1.95.230:8080/')], contextPath: '', onFailure: false, war: 'ajiolux/target/*.war' 
        }
      }
    }
    }
}
